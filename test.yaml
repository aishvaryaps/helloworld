formatVersion: 1
inputs:
  vmName:
    type: string
resources:
  ApplicationVM:
    type: Cloud.Machine
    metadata:
      layoutPosition:
        - 0
        - 0
    properties:
      image: ubuntu-16
      name: test
      flavor: micro
      newName: '${input.vmName}'
